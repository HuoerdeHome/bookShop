# 应用服务 WEB 访问端口
#server.port=8088
#tomcat服务器端口
server:
  port: 8088

#数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://@localhost:3306/springboot-web
    username: root
    password: 551493
##配置数据库连接参数
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://@localhost:3306/springboot-web
#spring.datasource.username=root
#spring.datasource.password=551493


  #配置mybatis的映射文件
#mybatis.mapper-locations=classpath*:mapper/*Mapper.xml
mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
  configuration:
    #开启控制台日志输出；
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

##下面这些内容是为了让MyBatis映射
##指定Mybatis的Mapper文件
#mybatis.mapper-locations=classpath:mappers/*xml
##指定Mybatis的实体目录
#mybatis.type-aliases-package=com.briup.demo.mybatis.entity

